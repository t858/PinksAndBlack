const client = createClient({
    space: process.env.const client = createClient({   space: process.env.CONTENTFUL_SPACE_ID,   accessToken: process.env.IS_PREVIEW === "true" ?     process.env.CONTENTFUL_PREVIEW_TOKEN :     process.env.CONTENTFUL_DELIVERY_TOKEN })  // Alternatively you can use the CDN API as follows... const baseUrl = process.env.IS_PREVIEW === "true" ? "preview.contentful.com" : "cdn.contentful.com"CONTENTFUL_SPACE_ID,
    accessToken: process.env.const client = createClient({   space: process.env.CONTENTFUL_SPACE_ID,   accessToken: process.env.IS_PREVIEW === "true" ?     process.env.CONTENTFUL_PREVIEW_TOKEN :     process.env.CONTENTFUL_DELIVERY_TOKEN })  // Alternatively you can use the CDN API as follows... const baseUrl = process.env.IS_PREVIEW === "true" ? "preview.contentful.com" : "cdn.contentful.com"IS_PREVIEW === "true" ?
      process.env.const client = createClient({   space: process.env.CONTENTFUL_SPACE_ID,   accessToken: process.env.IS_PREVIEW === "true" ?     process.env.CONTENTFUL_PREVIEW_TOKEN :     process.env.CONTENTFUL_DELIVERY_TOKEN })  // Alternatively you can use the CDN API as follows... const baseUrl = process.env.IS_PREVIEW === "true" ? "preview.contentful.com" : "cdn.contentful.com"CONTENTFUL_PREVIEW_TOKEN :
      process.env.const client = createClient({   space: process.env.CONTENTFUL_SPACE_ID,   accessToken: process.env.IS_PREVIEW === "true" ?     process.env.CONTENTFUL_PREVIEW_TOKEN :     process.env.CONTENTFUL_DELIVERY_TOKEN })  // Alternatively you can use the CDN API as follows... const baseUrl = process.env.IS_PREVIEW === "true" ? "preview.contentful.com" : "cdn.contentful.com"CONTENTFUL_DELIVERY_TOKEN
  })
  
  // Alternatively you can use the CDN API as follows...
  const baseUrl = process.env.const client = createClient({   space: process.env.CONTENTFUL_SPACE_ID,   accessToken: process.env.IS_PREVIEW === "true" ?     process.env.CONTENTFUL_PREVIEW_TOKEN :     process.env.CONTENTFUL_DELIVERY_TOKEN })  // Alternatively you can use the CDN API as follows... const baseUrl = process.env.IS_PREVIEW === "true" ? "preview.contentful.com" : "cdn.contentful.com"IS_PREVIEW === "true" ? "preview.contentful.com" : "cdn.contentful.com"